<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_height"
    android:background="#174f94"
    android:gravity="bottom"
    android:orientation="vertical"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:theme="@style/ThemeOverlay.AppCompat.Dark">

    <!-- Контейнер для изображений -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- Первое изображение -->
        <ImageView
            android:id="@+id/imageView"
            android:layout_width="81dp"
            android:layout_height="78dp"
            android:paddingTop="@dimen/nav_header_vertical_spacing"
            app:srcCompat="@drawable/ic_college_logo" />

        <!-- Второе изображение -->
        <ImageView
            android:id="@+id/imageViewTG"
            android:layout_width="43dp"
            android:layout_height="46dp"
            android:paddingLeft="8dp"
            android:clickable="true"
            android:focusable="true"
            app:srcCompat="@drawable/ic_tg" />

        <!-- Третье изображение -->
        <ImageView
            android:id="@+id/imageViewVK"
            android:layout_width="41dp"
            android:layout_height="46dp"
            android:clickable="true"
            android:focusable="true"
            android:paddingLeft="8dp"
            app:srcCompat="@drawable/ic_wk" />

    </LinearLayout>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/nav_header_vertical_spacing"
        android:text="Университетский колледж информационных технологий"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1" />

    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="" />

</LinearLayout>

